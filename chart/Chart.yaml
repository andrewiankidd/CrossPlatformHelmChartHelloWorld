apiVersion: v2
name: cross-platform-hello
version: 0.5.0
appVersion: "0.5.0"
description: >-
  Cross-platform hello world chart for Crosspose. Deploys a Linux service and
  a Windows service side by side, each validating connectivity to shared
  infrastructure (MSSQL, Service Bus, Azure Storage) and to each other.
